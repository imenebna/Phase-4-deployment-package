In this phase, I prepared a deployable application for my fine tuned BART summarization model. I created a Streamlit interface that loads the saved model and tokenizer, accepts user text as input, and generates abstractive summaries with configurable decoding settings. I also prepared the deployment package with the required dependencies and Docker support so the application can be tested locally and deployed on a production style server.
